{
  "name": "@vulos/wibbly-authority",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Runs a real magnetite AuthoritativeGame (compiled to wasm) in the browser as a client-side SingleRoom authority — the bottom rung of magnetite's topology ladder. Drives the mag_* sandbox ABI (init/restore/step/snapshot/view).",
  "license": "MIT",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  }
}
